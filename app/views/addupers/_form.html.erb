<%= form_for(@adduper) do |f| %>
  <% if @adduper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adduper.errors.count, "error") %> prohibited this adduper from being saved:</h2>

      <ul>
      <% @adduper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
